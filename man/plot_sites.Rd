% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_sites}
\alias{plot_sites}
\title{Plot site occupancy and detection rates}
\usage{
plot_sites(
  fit,
  map = TRUE,
  intercepts = TRUE,
  back_transform = TRUE,
  include_predictors = TRUE,
  restricted = TRUE,
  species = NULL,
  sites = NULL,
  regions = NULL,
  ndraws = NULL,
  seed = NULL,
  ...
)
}
\arguments{
\item{fit}{A fitted model object from \code{\link[=occARU]{occARU()}}.}

\item{map}{\code{logical}. If \code{TRUE} (default), plot site effects summarised with
posterior medians on a map using UTM coordinates. Requires site coordinates
to have been supplied to \code{\link[=make_data]{make_data()}}. If \code{FALSE}, or if no coordinates
are present, site effects are plotted with
\code{\link[ggdist:stat_pointinterval]{ggdist::stat_pointinterval()}}.}

\item{intercepts}{\code{logical}. If \code{TRUE} (default), species-level baseline log
detection rates are added to the site effects. If \code{FALSE}, only the site
deviations are plotted on the log scale.}

\item{back_transform}{\code{logical}. Only used when \code{intercepts = TRUE}. If \code{TRUE}
(default), log detection rates are back-transformed to the natural scale
via \code{exp()}. If \code{FALSE}, values are left on the log scale.}

\item{include_predictors}{\code{logical}. If \code{TRUE} (default), includes predictors
in the site effects, if included. If \code{FALSE}, only plots the random
effects.}

\item{restricted}{\code{logical}. If \code{TRUE} (default), when \code{include_predictors} is
\code{FALSE}, plots random site effects with orthogonal projection, i.e.,
\eqn{(\boldsymbol{I} - \boldsymbol{P_{X_2}}) \boldsymbol{\iota}}, where
\eqn{\boldsymbol{I} - \boldsymbol{P_{X_2}}} is the orthogonal complement of
the column space of the site design matrix. If \code{FALSE}, plots random
effects without orthogonal projection, i.e., \eqn{\boldsymbol{\iota}} only.
Has no effect when \code{include_predictors} is \code{TRUE} as the linear predictor
is unaffected by orthogonal projection.}

\item{species}{\code{character}. Vector of species to plot. If \code{NULL} (default),
all species are plotted. Must be one of \code{attr(occARU_data, "species")}.}

\item{sites}{\code{character}. Vector of sites to plot. If \code{NULL} (default), all
sites are plotted. Must be one of \code{attr(occARU_data, "sites")}.}

\item{regions}{\code{character}. Vector of regions to plot. If \code{NULL} (default),
all regions are plotted. Must be one of \code{attr(occARU_data, "regions")}.}

\item{ndraws}{Positive integerish. Number of draws to use for plotting,
passed to \code{\link[tidybayes:spread_rvars]{tidybayes::spread_rvars()}}. Default: \code{NULL} (uses all draws).}

\item{seed}{Positive numeric. Seed to use when subsampling draws when
\code{ndraws} is not \code{NULL}. Default: random integer.}

\item{...}{Additional arguments passed to \code{\link[ggplot2:geom_point]{ggplot2::geom_point()}} when
\code{map = TRUE} and  \code{\link[ggdist:stat_pointinterval]{ggdist::stat_pointinterval()}} when \code{map = FALSE}.}
}
\value{
A \code{ggplot} object with occARU-specific attributes attached:
\describe{
\item{\code{plot_data}}{The tibble used to produce the plot.}
}
}
\description{
Plots species-level occupancy and detection rates combining site predictors
(if included via \code{\link[=make_data]{make_data()}}) and site random effects (\code{iota}).
}
\details{
If \code{map = TRUE} and site coordinates are present in the fitted object,
effects are displayed as points sized by the magnitude of the detection rate
on a map; otherwise site effects are plotted as point-intervals. When
\code{latent = TRUE} was set in \code{\link[=occARU]{occARU()}}, sites with median posterior
occupancy of 0 are shown as red crosses. When \code{latent = FALSE}, detection
rates are weighted by occupancy probability (\code{inv_logit(logit_psi[i, s])}).
}
\seealso{
\code{\link[=occARU]{occARU()}}, \code{\link[=plot_intercepts]{plot_intercepts()}}, \code{\link[=plot_coefficients]{plot_coefficients()}},
\code{\link[=plot_surveys]{plot_surveys()}}, \code{\link[=plot_correlations]{plot_correlations()}}, \code{\link[=plot_partitions]{plot_partitions()}},
\code{\link[=plot_realised]{plot_realised()}}
}
