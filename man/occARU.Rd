% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/occARU.R
\name{occARU}
\alias{occARU}
\title{Fit the occARU model}
\usage{
occARU(
  data,
  prior = set_priors(verbose = FALSE),
  stan_file = NULL,
  random = list(site = gp(), survey = gp()),
  overdispersion = c("none", "nb", "olre"),
  variance_decomposition = c("dirichlet", "logistic-normal"),
  ppc = c("Q", "y", "both", "none"),
  latent = TRUE,
  loo_draws = 100L,
  init = 0.1,
  pathfinder_args = list(),
  threads = 1L,
  grainsize = 1L,
  ...
)
}
\arguments{
\item{data}{A \code{occARU_data} object produced by \code{\link[=make_data]{make_data()}}.}

\item{prior}{An \code{occARU_priors} object produced by \code{\link[=set_priors]{set_priors()}}. If
omitted, default priors are used.}

\item{stan_file}{\code{character}. Path to a custom Stan file. If \code{NULL}
(default), uses the built-in occARU model. Intended for advanced users who
have modified the Stan programs; note that custom models will likely
require corresponding changes to the output of \code{\link[=make_data]{make_data()}}.}

\item{random}{A named \code{list} specifying random effect structures for site and
survey effects on detection. For multiseason models, optionally accepts
season effects on detection (\code{season}) and site (\code{site_occ}) and season
(\code{season_occ}) effects on occupancy. Must be one of:
\itemize{
\item \code{\link[=gp]{gp()}}, which fits a (multispecies) Gaussian process. Default for site
and survey effects. Also the default for season effects on both detection
and occupancy and occupancy site effects in dynamic models.
\item \code{"mvn"}, which fits an unstructured (multispecies) normal.
\item \code{"none"}, which omits random effects entirely.
}}

\item{overdispersion}{\code{character}. Overdispersion model for the observation
process. One of \code{"none"} (Poisson, default), \code{"nb"} (negative binomial),
or \code{"olre"} (multispecies observation-level random effects).}

\item{variance_decomposition}{\code{character}. Prior for variance partitions.
One of \code{"dirichlet"} (default) or \code{"logistic-normal"}.}

\item{ppc}{\code{character}. Posterior predictive checks to compute. One of \code{"Q"}
(default), \code{"y"}, \code{"both"}, or \code{"none"}. \code{"y"} returns the full
\verb{[K, I, J, S]} prediction array (\code{yrep}); \code{"Q"} returns only aggregated
counts \verb{[K, I, S]} (\code{Qrep}). For large datasets, \code{"Q"} or \code{"none"} can
substantially reduce memory usage and sampling time.}

\item{latent}{\code{logical}. If \code{TRUE} (default), latent occupancy states \code{z}
are recovered for each species using the forward-backward sampling
algorithm.}

\item{loo_draws}{Non-negative integer. Number of Monte Carlo draws for
marginal log-likelihood estimation of site-level random effects and/or
Poisson OLREs for PSI-LOO-CV via \code{\link[loo:loo]{loo::loo()}}. Default: \code{100}, which
produces an additional \verb{[S, I]} matrix \code{log_lik2} by marginalising over
site effects (and OLRE residuals if applicable) via Monte Carlo
integration. \code{log_lik2} is recommended over \code{log_lik} for PSIS-LOO-CV as it
produces better Pareto-k diagnostics. Set to \code{0} to disable, returning only
\code{log_lik}. Only used with site random effects or \code{overdispersion = "olre"}. Note: even high values (10K) seem inadequate for OLREs.}

\item{init}{\code{numeric}, \code{character}, or \code{list}. Initialisation strategy passed
to \link[cmdstanr:CmdStanModel]{cmdstanr::CmdStanModel}\verb{$sample()}. One of:
\itemize{
\item A numeric scalar (default). Initialises all parameters uniformly in
\verb{[-init, init]}. Default: 0.1.
\item \code{"pathfinder"}. Use Pathfinder to generate initial values (see
\link[cmdstanr:CmdStanModel]{cmdstanr::CmdStanModel}\verb{$pathfinder()}).
\item A named list with custom initial values passed directly to
\link[cmdstanr:CmdStanModel]{cmdstanr::CmdStanModel}\verb{$sample()}.
}}

\item{pathfinder_args}{Named list of additional arguments passed to
\link[cmdstanr:CmdStanModel]{cmdstanr::CmdStanModel}\verb{$pathfinder()} when \code{init = "pathfinder"}.
Overrides occARU-specific defaults (\code{refresh = 0}, \code{init = 0.1},
\code{sig_figs = 14}, \code{num_threads = chains}, \code{num_paths = chains},
\code{max_lbfgs_iters = 200}, \code{psis_resample = FALSE}).}

\item{threads}{Positive integer. Number of threads for within-chain
parallelisation via \code{reduce_sum()}. Default: \code{1} (no parallelisation).
The total number of threads used is \code{threads * chains}, so for optimal
performance set \code{threads = floor(available_cores / chains)}. For example,
8 cores with 4 chains gives \code{threads = 2}.}

\item{grainsize}{Positive integer. Chunk size for within-chain
parallelisation when \code{threads > 1}. For data with multiple regions, chunks
are number of regions; otherwise it is number of sites in each slice.
Default: \code{1}, which lets Stan automatically determine the optimal chunk
size. Increase if you have many sites or regions and want to reduce
parallelisation overhead. See the
\href{https://mc-stan.org/docs/stan-users-guide/parallelization.html#reduce-sum-grainsize}{Stan
User's Guide} for details on tuning grainsize.}

\item{...}{Additional arguments passed to
\link[cmdstanr:CmdStanModel]{cmdstanr::CmdStanModel}\verb{$sample()}. Uses parallel chains by default.
All other sampling arguments use Stan defaults.}
}
\value{
An \code{occARU_fit} object, which extends the
\href{https://mc-stan.org/cmdstanr/reference/CmdStanFit.html}{CmdStanFit}
class with occARU-specific attributes:
\describe{
\item{\code{stan_data}}{The full Stan data list passed to the model,
including prior hyperparameters.}
\item{\code{occARU_data}}{The original \code{occARU_data} object from
\code{\link[=make_data]{make_data()}}.}
}
}
\description{
Fits the occARU model to data prepared by \code{\link[=make_data]{make_data()}} in Stan. Requires
CmdStan >= 2.36.0, which can be installed with \code{\link[=setup_occARU]{setup_occARU()}}.
Automatically determines number of species in the supplied data and whether a
single or multiseason model is required.
}
\seealso{
\code{\link[=make_data]{make_data()}}, \code{\link[=set_priors]{set_priors()}}, \code{\link[=gp]{gp()}}, \code{\link[=setup_occARU]{setup_occARU()}},
\code{\link[=pp_check.occARU_fit]{pp_check.occARU_fit()}}, \link[cmdstanr:CmdStanMCMC]{cmdstanr::CmdStanMCMC} for methods on the fitted
model object. The statistical model is described in \code{vignette("model")}.
}
