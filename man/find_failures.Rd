% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find_failures.R
\name{find_failures}
\alias{find_failures}
\title{Find potential ARU failure periods}
\usage{
find_failures(
  df,
  locationID = locationID,
  eventStart = eventStart,
  buffer_days
)
}
\arguments{
\item{df}{A dataframe of records. Must contain columns \code{locationID} and
\code{eventStart} (or equivalents).}

\item{locationID}{<\code{\link[rlang:args_data_masking]{data-masking}}> Column
name for site identifiers. Default: \code{locationID}.}

\item{eventStart}{<\code{\link[rlang:args_data_masking]{data-masking}}> \code{POSIXt}.
Observation timestamps. Default: \code{eventStart}.}

\item{buffer_days}{Positive integer. Number of days after the last detection
and before the next detection to exclude from the inferred failure period.
For example, \code{buffer_days = 2} means the failure period starts 2 days
after the last detection and ends 2 days before the next detection.}
}
\value{
A dataframe with columns \code{locationID}, \code{failureStart}, and
\code{failureEnd}, one row per inferred failure period, with \code{buffer_days}
stored as attribute.
}
\description{
Identifies potential failure periods by searching for unusually long gaps
between consecutive detections at each site. Best used on a dataframe of
all records including null records, as a suitable gap threshold requires
knowledge of expected detection rates.
}
