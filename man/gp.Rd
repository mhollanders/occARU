% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/random.R
\name{gp}
\alias{gp}
\title{Specify Gaussian process in occARU models}
\usage{
gp(
  kernel = c("exp_quad_cov", "matern32", "matern52"),
  species_length_scales = FALSE,
  periodic = FALSE,
  period = NULL
)
}
\arguments{
\item{kernel}{\code{character}. The type of GP kernel to use. Must be one of
\code{"exp_quad"} (exponentiatated quadratic), \code{"matern32"} (Matern 3/2), or
\code{"matern52"} (Matern 5/2).}

\item{species_length_scales}{\code{logical}. If \code{TRUE}, species-specific length
scales are estimated for that GP, each drawn independently from the shared
length scale priors. Note that enabling this requires additional Cholesky
decompositions per species and GP, which can substantially increase
sampling time. If \code{FALSE} (default), only one length scale is estimated
with one Cholesky decomposition performed per GP. Only used when multiple
species are included.}

\item{periodic}{\code{logical}. If \code{TRUE}, a periodic kernel is added to the
kernel for survey effects. Default: \code{FALSE}.}

\item{period}{Positive numeric. Period length in survey units (i.e. number
of survey periods per cycle). Only used when \code{periodic = TRUE}. Defaults to
\code{365 / survey_length}, corresponding to an annual cycle. For example, with
\code{survey_length = 7} the default is \code{period = 52.1}. Override if your data
span a different temporal cycle.}
}
\value{
A \code{gp} object (a named list with class \code{"gp"}) for use in the
\code{random} argument of \code{\link[=occARU]{occARU()}}. Contains the following elements:
\describe{
\item{\code{random}}{Integer. Always \code{1L}, indicating a random effect.}
\item{\code{kernel}}{Integer. Kernel index (1 = exponentiated quadratic,
2 = Matern 3/2, 3 = Matern 5/2).}
\item{\code{species_length_scales}}{Integer. Whether to estimate
species-specific length scales (0 or 1).}
\item{\code{periodic}}{Integer. Whether a periodic kernel component is
included (0 or 1).}
\item{\code{period}}{Numeric. Period length in survey units, or \code{0} if not
periodic.}
}
}
\description{
Specify Gaussian process (GP) random effects in occARU models, with a choice
of kernel and whether to estimate species-level length-scales.
}
\seealso{
\code{\link[=occARU]{occARU()}}, \code{\link[=set_priors]{set_priors()}}
}
\keyword{internal}
