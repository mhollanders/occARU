% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_data.R
\name{plot_observations}
\alias{plot_observations}
\title{Plot observations}
\usage{
plot_observations(data, by_region = FALSE, species = NULL, regions = NULL, ...)
}
\arguments{
\item{data}{An \code{occARU_data} object from \code{\link[=make_data]{make_data()}}.}

\item{by_region}{\code{logical.} Whether to summarise observations by region, if
multiple regions were included. Default: \code{FALSE}.}

\item{species}{\code{character}. Vector of species to plot. If \code{NULL} (default),
all species are plotted. Must be one of \code{attr(occARU_data, "species")}.}

\item{regions}{\code{character}. Vector of regions to use. If \code{NULL} (default),
all regions are used. Must be one of \code{attr(occARU_data, "regions")}.}

\item{...}{Additional arguments passed to \code{\link[ggplot2:geom_point]{ggplot2::geom_point()}}.}
}
\value{
A \code{ggplot} object with occARU-specific attributes attached:
\describe{
\item{\code{plot_data}}{The tibble used to produce the plot.}
}
}
\description{
Plot observations of the data used in the occARU model, aggregated to the
chosen survey length and summed across sites.
}
\seealso{
\code{\link[=make_data]{make_data()}} \code{\link[=plot_deployments]{plot_deployments()}}
}
