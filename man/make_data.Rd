% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_data.R
\name{make_data}
\alias{make_data}
\title{Prepare data for the occARU model}
\usage{
make_data(
  deployments,
  observations,
  failures = NULL,
  locationID = locationID,
  deploymentStart = deploymentStart,
  deploymentEnd = deploymentEnd,
  latitude = latitude,
  longitude = longitude,
  region = region,
  season = season,
  eventStart = eventStart,
  scientificName = scientificName,
  count = count,
  failureStart = failureStart,
  failureEnd = failureEnd,
  survey_length = 1L,
  thin_minutes = 30,
  day_start = c("midday", "midnight"),
  occupancy_site_predictors = NULL,
  detection_site_predictors = NULL,
  survey_predictors = NULL,
  date = date,
  summary_functions = NULL,
  scale_predictors = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{deployments}{A dataframe of deployment information, one row per
site (and potentially season). Must contain columns \code{locationID},
\code{deploymentStart}, and \code{deploymentEnd} (or equivalents specified via the
corresponding arguments). Optionally, \code{latitude} and \code{longitude} columns
enable the spatial Gaussian process. If multiple seasons, must also contain
column \code{season}.}

\item{observations}{A dataframe of observation records. Must contain
columns \code{locationID}, \code{eventStart}, \code{scientificName}, and \code{count}
(or equivalents specified via the corresponding arguments). If multiple
seasons, must also contain column \code{season}.}

\item{failures}{Optional dataframe of ARU failure periods. Must contain
columns \code{locationID}, \code{failureStart}, and \code{failureEnd}, with each row
corresponding to one failure period at a \code{locationID} from \code{failureStart}
to \code{failureEnd} (inclusive). See \code{\link[=find_failures]{find_failures()}}.}

\item{locationID}{<\code{\link[rlang:args_data_masking]{data-masking}}> Column
name for sites (ARUs). Retains levels if supplied as factor.  Default:
\code{locationID}.}

\item{deploymentStart}{<\code{\link[rlang:args_data_masking]{data-masking}}> \code{Date}.
Column name for deployment start dates in \code{deployments}. Default:
\code{deploymentStart}.}

\item{deploymentEnd}{<\code{\link[rlang:args_data_masking]{data-masking}}> \code{Date.}
Column name for deployment end dates in \code{deployments}. Default:
\code{deploymentEnd}.}

\item{latitude}{<\code{\link[rlang:args_data_masking]{data-masking}}> \code{numeric}.
Column name for WGS84 latitude in \code{deployments}. If omitted alongside
\code{longitude}, no spatial Gaussian process is fitted. Default: \code{latitude}.}

\item{longitude}{<\code{\link[rlang:args_data_masking]{data-masking}}> \code{numeric}.
Column name for WGS84 longitude in \code{deployments}. Default: \code{longitude}.}

\item{region}{<\code{\link[rlang:args_data_masking]{data-masking}}> Optional column in
\code{deployments} specifying region, defined as a cluster of ARUs. Leads to
faster model fits when spatial site effects are included in \code{\link[=occARU]{occARU()}}.
If the column is not present in \code{deployments}, all observations are treated
as a single region. Default: \code{region}.}

\item{season}{<\code{\link[rlang:args_data_masking]{data-masking}}> Optional column
specifying season in \code{deployments}. The column must be a factor to ensure
correct ordering. If the column is not present, a single season is assumed.
Default: \code{season}. See \code{\link[=find_seasons]{find_seasons()}}.}

\item{eventStart}{<\code{\link[rlang:args_data_masking]{data-masking}}> \code{POSIXt}.
Column name for observation timestamps in \code{observations}. Default:
\code{eventStart}.}

\item{scientificName}{<\code{\link[rlang:args_data_masking]{data-masking}}> Column
name for species names in \code{observations}. Retains levels if supplied as
factor. Default: \code{scientificName}.}

\item{count}{<\code{\link[rlang:args_data_masking]{data-masking}}> \code{integerish}.
Column name for number of individuals per observation record. Default:
\code{count}.}

\item{failureStart}{<\code{\link[rlang:args_data_masking]{data-masking}}> \code{Date}.
Column name for failure start dates in \code{failures}. Default:
\code{failureStart}.}

\item{failureEnd}{<\code{\link[rlang:args_data_masking]{data-masking}}> \code{Date}. Column
name for failure end dates (inclusive) in \code{failures}. Default:
\code{failureEnd}.}

\item{survey_length}{Positive integer. Defines the length of each survey
period in days. Observations are aggregated within each survey period by
summing \code{count}, and recording effort (\code{Delta}) is computed as the fraction
of the survey length the ARU was active. For example, \code{survey_length = 7}
aggregates to weekly survey periods, with \code{Delta} ranging from 0
(ARU failed all week) to 1 (ARU active all week). Longer periods reduce the
number of surveys \code{J} but increase the counts per survey, trading off
temporal resolution against model complexity and the closure assumption
within a survey period. Default: \code{1L}.}

\item{thin_minutes}{Non-negative numeric. If supplied, observations within
\code{thin_minutes} minutes of each other (per site and species) are thinned to
a single observation, retaining the record with the highest \code{count}.
Thinning is performed via \code{\link[=thin_observations]{thin_observations()}}. Default: \code{30}.}

\item{day_start}{Whether survey days start at \code{"midnight"} or \code{"midday"}.
Default: \code{"midday"}.}

\item{occupancy_site_predictors}{Optional dataframe of site-level
covariates for the occupancy submodel. Must contain a \code{locationID} column
with the same entries as \code{deployments}. Predictor columns must be \code{numeric}
(continuous), \code{factor} (unordered categorical), or \verb{ordered factor}
(ordinal). If multiple seasons, each \code{locationID} requires a value for
each \code{season} it was deployed.}

\item{detection_site_predictors}{Optional dataframe of site-level
covariates for the detection submodel. Same column-type rules as
\code{occupancy_site_predictors}. If identical to
\code{occupancy_site_predictors}, the same matrices are reused.}

\item{survey_predictors}{Optional dataframe of site-by-survey level
covariates, with one row per site and date. Must contain \code{locationID}
and \code{date} columns. Predictor columns follow the same type rules as the
site-level predictor dataframes. Must cover the full deployment period for
each \code{locationID}.}

\item{date}{<\code{\link[rlang:args_data_masking]{data-masking}}> Column name for
dates in \code{survey_predictors}. Default: \code{date}.}

\item{summary_functions}{An optional named list mapping continuous survey
predictor column names to summary functions, used when aggregating survey
predictors over \code{survey_length}-length periods. Each value can be a
function name as a string (e.g. \code{"sum"}) or a function object (e.g. \code{sum}).
Numeric predictors not named in \code{summary_functions} are summarised with
\code{mean}; categorical and ordinal predictors are summarised with the
modal value. Default: \code{NULL}.}

\item{scale_predictors}{Logical. If \code{TRUE}, continuous predictors are scaled
to zero mean and unit variance. Survey predictors are scaled using
parameters derived from site-averaged values per survey period (a \verb{[P, J]}
matrix) rather than the raw \verb{[I, P, J]} array, so that spatial variation
across sites does not inflate the scaling. Scaling parameters (means and
SDs) are stored as an attribute. Default: \code{TRUE}.}

\item{verbose}{Logical. If \code{TRUE} (default), prints data.}
}
\value{
A named list of class \code{"occARU_data"} containing all inputs
required by the occARU Stan model, except for model specification
arguments which are added by \code{\link[=occARU]{occARU()}}. The list contains:
\describe{
\item{\code{I}}{Number of sites (ARUs).}
\item{\code{R}}{Number of regions (groups of sites).}
\item{\code{J}}{Number of survey periods (maximum).}
\item{\code{K}}{Number of seasons.}
\item{\code{S}}{Number of species.}
\item{\code{tau}}{Interval length in years between end of previous deploymment
and start of current deployment (if multiseason).}
\item{\code{dyn}}{Indicator for dynamic occupancy, when at least one site was
deployed over multiple seasons.}
\item{\code{Delta}}{\verb{[K, J, I]} array of recording effort (0-1).}
\item{\code{y}}{\verb{[K, I, J, S]} array of detection counts.}
\item{\code{XY}}{\verb{[I, 2]} matrix of UTM coordinates in km, or zeros if
coordinates not supplied.}
\item{\code{P}}{Integer vector of length 3: number of continuous predictors
for occupancy, and site and survey detection.}
\item{\code{P_cat}}{Integer vector of length 3: number of categorical
predictors for each component.}
\item{\code{P_ord}}{Integer vector of length 3: number of ordinal predictors
for each component.}
\item{\code{X1}}{\verb{[K, I, P[1]]} occupancy continuous design array.}
\item{\code{X_cat1}}{\verb{[K, I, P_cat[1]]} occupancy categorical integer
array.}
\item{\code{X_ord1}}{\verb{[K, I, P_ord[1]]} occupancy ordinal integer array.}
\item{\code{X2}}{\verb{[K, I, P[2]]} site-level detection continuous design
array.}
\item{\code{X_cat2}}{\verb{[K, I, P_cat[2]]} site-level detection categorical
integer array.}
\item{\code{X_ord2}}{\verb{[K, I, P_ord[2]]} site-level detection ordinal integer
array.}
\item{\code{X3}}{\verb{[K, I, J, P[3]]} site-by-survey level detection continuous
array.}
\item{\code{X_cat3}}{\verb{[K, I, J, P_cat[3]]} site-by-survey categorical
integer array.}
\item{\code{X_ord3}}{\verb{[K, I, J, P_ord[3]]} site-by-survey survey ordinal
integer array.}
}
The object also carries the following attributes, accessible via
\code{\link[=attr]{attr()}}:
\describe{
\item{\code{deployments}}{The processed \code{deployments}.}
\item{\code{observations}}{The processed and thinned \code{observations} aggregated
to the chosen survey length.}
\item{\code{sites}}{Character vector of site identifiers.}
\item{\code{regions}}{Character vector of region identifiers.}
\item{\code{surveys}}{tibble of start dates and indices for each survey
period per season.}
\item{\code{seasons}}{Character vector of season identifiers.}
\item{\code{species}}{Character vector species names.}
\item{\code{utm_crs}}{Character. PROJ string of the UTM coordinate reference
system used to transform site coordinates, or \code{NULL} if no coordinates
were supplied.}
\item{\code{scaling}}{tibble of means and standard deviations used to
standardise continuous predictors, or \code{NULL} if
\code{scale_predictors = FALSE}.}
\item{\code{levels}}{Named list of category levels for categorical and ordinal
predictors.}
\item{\code{reference_dates}}{First deploymentStart per season.}
\item{\code{survey_length}}{}
\item{\code{thin_minutes}}{}
\item{\code{day_start}}{}
}
}
\description{
Transforms raw deployment and observation data into a named list suitable
for passing to \code{\link[=occARU]{occARU()}}. Follows the
\href{https://tdwg.github.io/camtrap-dp/}{camtrapDP} data format by default.
Site coordinates are automatically projected from WGS84 latitude/longitude
to UTM (km), with the zone auto-detected from the mean longitude.
}
\seealso{
\code{\link[=occARU]{occARU()}}, \code{\link[=plot_deployments]{plot_deployments()}}, \code{\link[=plot_observations]{plot_observations()}},
\code{\link[=thin_observations]{thin_observations()}}, \code{\link[=find_failures]{find_failures()}}, \code{\link[=find_seasons]{find_seasons()}}
The model is described in detail in \code{vignette("model")}.
}
