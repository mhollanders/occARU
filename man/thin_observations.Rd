% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/thin_observations.R
\name{thin_observations}
\alias{thin_observations}
\title{Thin observations}
\usage{
thin_observations(
  observations,
  locationID = locationID,
  scientificName = scientificName,
  eventStart = eventStart,
  count = count,
  thin_minutes = 30
)
}
\arguments{
\item{observations}{A dataframe of observation records. Must contain
columns \code{locationID}, \code{eventStart}, \code{scientificName}, and \code{count}
(or equivalents specified via the corresponding arguments).}

\item{locationID}{<\code{\link[rlang:args_data_masking]{data-masking}}> Column
name for sites (ARUs). Default: \code{locationID}.}

\item{scientificName}{<\code{\link[rlang:args_data_masking]{data-masking}}> Column
name for species names. Default: \code{scientificName}.}

\item{eventStart}{<\code{\link[rlang:args_data_masking]{data-masking}}> \code{POSIXt}.
Column name for observation timestamps. Default: \code{eventStart}.}

\item{count}{<\code{\link[rlang:args_data_masking]{data-masking}}> \code{integerish}.Column
name for number of individuals per observation record. Default: \code{count}.}

\item{thin_minutes}{Non-negative numeric. Observations within \code{thin_minutes}
minutes of each other (per site and species) are thinned to a single
observation, retaining the record with the highest \code{count}. Default: \code{30}.}
}
\value{
A dataframe of thinned observation records, sorted by \code{locationID},
\code{scientificName}, and \code{eventStart}, or the original dataframe if
\code{thin_minutes = 0}, with \code{thin_minutes} stored as attribute.
}
\description{
Removes redundant detections by grouping consecutive records within
\code{thin_minutes} minutes of each other into clusters (per site and species),
retaining only the record with the highest count from each cluster.
}
\seealso{
\code{\link[=make_data]{make_data()}}
}
